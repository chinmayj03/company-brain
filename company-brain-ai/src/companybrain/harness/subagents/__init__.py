"""Sub-agent package — specialised agents that the harness can spawn.

Phase-6 introduces the first non-extraction sub-agent: ``browser_verifier``,
which headless-Chromes a frontend repo and reports drift against the brain's
``ApiEndpoint`` set.
"""
